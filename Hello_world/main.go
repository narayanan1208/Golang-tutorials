package main

import "fmt"

func main() {
	fmt.Println("Hello")
}

// go run filename
// go env GOPATH
// go is case insensitive
// types - String, Bool, Integer, Floating, Complex, Array, Slices, Maps, Structs(instead of Class), Pointers
